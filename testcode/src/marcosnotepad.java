public class marcosnotepad {
  public static void main(String[] args) {
    // chapter 1
    // primitives:
    // (int) Byte(8bit) -> short(16bit) -> int(32bit) -> long(64bit)
    // (定義）(True / false) Boolean(1 bit)
    // (charaters(Ascii codes)) char(16 bit)
    // (小數點）float(32 bit) -> double(64 bit)

    // chapter 2
    // String (methods):
    // equals()
    // length()
    // charAt()


  }
}
